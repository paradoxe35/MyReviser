name: Build and Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      debug_enabled:
        description: "Enable debug mode"
        type: boolean
        required: false
        default: false

env:
  GO_VERSION: "1.24"
  RUST_VERSION: "stable"
  # ggml decides its SIMD level from whether CMake thinks it is cross compiling,
  # which differs per job: the native legs (Linux, macOS arm64) compile
  # -march=native and SIGILL on any user CPU older than the runner's, while the
  # cross legs (Windows, macOS amd64) disable every tier and transcribe several
  # times slower than they need to. Pin one baseline for every artifact instead.
  # AVX2 means x86 machines from ~2013 on; the flags are inert on arm64.
  TRANSCRIBE_CMAKE_ARGS: >-
    -DGGML_NATIVE=OFF
    -DGGML_SSE42=ON
    -DGGML_AVX=ON
    -DGGML_AVX2=ON
    -DGGML_FMA=ON
    -DGGML_F16C=ON
    -DGGML_BMI2=ON

jobs:
  build-linux:
    strategy:
      matrix:
        arch: [amd64]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ env.RUST_VERSION }}
          override: true

      - name: Cache Rust dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            rust-ffi/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            libasound2-dev \
            libgl1-mesa-dev \
            libwayland-dev \
            xorg-dev \
            libx11-dev \
            libxkbfile-dev \
            libxtst-dev \
            libxdo-dev \
            libxi-dev \
            libpng-dev \
            libjpeg-dev \
            libxinerama-dev \
            libxcb-xkb-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-x11-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxrender-dev \
            libxfixes-dev \
            libxxf86vm-dev \
            libxkbcommon-dev

      - name: Install Fyne CLI
        run: go install fyne.io/tools/cmd/fyne@v1.7.2

      - name: Install dependencies
        run: go mod download

      - name: Build Rust FFI library
        run: |
          bash scripts/build-rust-ffi.sh
          mkdir -p lib
          cp rust-ffi/target/release/libencre_ffi.a lib/

      - name: Build application with Fyne
        env:
          CGO_ENABLED: 1
          GOOS: linux
          GOARCH: ${{ matrix.arch }}
        run: |
          # Extract version from tag or use 0.0.0 for manual dispatch (fyne requires x.y.z format)
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.0"
          fi
          BUILD_NUMBER="${GITHUB_RUN_NUMBER}"

          echo "Building version: ${VERSION} (build: ${BUILD_NUMBER})"

          # Use fyne package with version info (creates Encre.tar.xz for Linux)
          fyne package --icon assets/icon.png --name Encre --release \
            --app-version "${VERSION}" \
            --app-build "${BUILD_NUMBER}"

          # The CLI wraps the tree in a directory named after the binary, which older versions did
          # not, so the binary is located by its path inside the archive rather than at a fixed one.
          mkdir -p pkg
          tar -xf Encre.tar.xz -C pkg

          BINARY=$(find pkg -path "*/usr/local/bin/*" -type f -perm -u+x | head -n 1)
          if [ -z "$BINARY" ]; then
            echo "Error: no binary in Encre.tar.xz"
            tar -tf Encre.tar.xz
            exit 1
          fi

          echo "Found binary: $BINARY"
          mv "$BINARY" encre-linux-${{ matrix.arch }}

          rm -rf pkg Encre.tar.xz

      - name: Create portable archive
        run: |
          mkdir -p dist/portable
          cp encre-linux-${{ matrix.arch }} dist/portable/encre
          chmod +x dist/portable/encre
          if [ -f assets/icon.png ]; then
            cp assets/icon.png dist/portable/
          fi
          tar -czf encre-linux-${{ matrix.arch }}.tar.gz -C dist/portable .

      - name: Create AppImage
        if: matrix.arch == 'amd64'
        run: |
          # Download linuxdeploy
          wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          chmod +x linuxdeploy-x86_64.AppImage

          # Create AppDir structure with proper hierarchy
          mkdir -p AppDir/usr/bin
          mkdir -p AppDir/usr/share/applications
          mkdir -p AppDir/usr/share/icons/hicolor/256x256/apps

          # Copy binary
          cp encre-linux-${{ matrix.arch }} AppDir/usr/bin/encre
          chmod +x AppDir/usr/bin/encre

          # Copy icon to proper location
          if [ -f assets/icon.png ]; then
            cp assets/icon.png AppDir/usr/share/icons/hicolor/256x256/apps/encre.png
            # Also copy to root for compatibility
            cp assets/icon.png AppDir/encre.png
          fi

          # Create desktop entry in proper location
          cat > AppDir/usr/share/applications/encre.desktop <<'EOF'
          [Desktop Entry]
          Name=Encre
          GenericName=Text Revision Tool
          Comment=AI-powered text revision tool
          Exec=encre
          Icon=encre
          Terminal=false
          Type=Application
          Categories=Utility;Office;
          Keywords=text;revision;ai;
          StartupNotify=true
          StartupWMClass=Encre Settings
          EOF

          # Also copy desktop file to root (required by linuxdeploy)
          cp AppDir/usr/share/applications/encre.desktop AppDir/encre.desktop

          # Create AppImage with proper desktop integration
          ARCH=x86_64 ./linuxdeploy-x86_64.AppImage --appdir AppDir --output appimage

          # Find and rename the generated AppImage (exclude linuxdeploy itself)
          APPIMAGE_FILE=$(find . -maxdepth 1 -name "*.AppImage" -type f ! -name "linuxdeploy-*" | head -n 1)
          if [ -n "$APPIMAGE_FILE" ]; then
            echo "Found AppImage: $APPIMAGE_FILE"
            mv "$APPIMAGE_FILE" encre-linux-${{ matrix.arch }}.AppImage
            chmod +x encre-linux-${{ matrix.arch }}.AppImage
          else
            echo "Error: AppImage file not found"
            echo "Available files:"
            ls -la *.AppImage 2>/dev/null || echo "No AppImage files found"
            exit 1
          fi

      - name: Create .deb package
        run: |
          mkdir -p dist/deb/DEBIAN
          mkdir -p dist/deb/usr/local/bin
          mkdir -p dist/deb/usr/share/applications
          mkdir -p dist/deb/usr/share/icons/hicolor/256x256/apps

          # Copy binary
          cp encre-linux-${{ matrix.arch }} dist/deb/usr/local/bin/encre
          chmod +x dist/deb/usr/local/bin/encre

          # Sanitize version for Debian (must start with digit)
          RAW_VERSION="${GITHUB_REF_NAME#v}"
          # If version doesn't start with digit, use 0.0.0 prefix
          if ! echo "$RAW_VERSION" | grep -q '^[0-9]'; then
            DEB_VERSION="0.0.0-${RAW_VERSION}"
          else
            DEB_VERSION="$RAW_VERSION"
          fi
          # Replace invalid chars with dash
          DEB_VERSION=$(echo "$DEB_VERSION" | sed 's/[^0-9a-zA-Z.]/-/g' | sed 's/^-//' | sed 's/-$//')
          # Fallback if empty
          [ -z "$DEB_VERSION" ] && DEB_VERSION="0.0.0-dev"
          echo "Debian version: $DEB_VERSION"

          # Create control file
          cat > dist/deb/DEBIAN/control <<EOF
          Package: encre
          Version: $DEB_VERSION
          Section: text
          Priority: optional
          Architecture: ${{ matrix.arch }}
          Maintainer: Paradoxe Ng <contact@pngwasi.me>
          Homepage: https://github.com/paradoxe35/encre
          Depends: libc6, libgl1, libx11-6, libxext6, libxcb1, libxinerama1, libxtst6, libxdo3, libxkbcommon0, libxi6, libxcursor1, libxrandr2, libxrender1, libxfixes3, libxxf86vm1
          Description: AI-powered text revision tool with Rust FFI
           Encre is a system tray application that provides global hotkeys
           for instant text enhancement using AI providers (OpenAI, Claude, Gemini).
           .
           Features:
            - Global hotkeys for instant text revision (Ctrl+Alt+Space)
            - Multiple AI provider support (OpenAI, Claude, Gemini)
            - System tray integration for quick access
            - Configurable system prompts and character limits
            - Non-destructive clipboard management
           .
           Built with Go and Rust FFI for optimal cross-platform performance.
           .
           License: MIT
          EOF

          # Copy icon if exists
          if [ -f assets/icon.png ]; then
            mkdir -p dist/deb/usr/share/icons/hicolor/256x256/apps
            cp assets/icon.png dist/deb/usr/share/icons/hicolor/256x256/apps/encre.png

            # Also copy to pixmaps as fallback
            mkdir -p dist/deb/usr/share/pixmaps
            cp assets/icon.png dist/deb/usr/share/pixmaps/encre.png
          fi

          # Create desktop entry
          cat > dist/deb/usr/share/applications/encre.desktop <<EOF
          [Desktop Entry]
          Name=Encre
          GenericName=Text Revision Tool
          Comment=AI-powered text revision tool
          Exec=/usr/local/bin/encre
          Icon=encre
          Terminal=false
          Type=Application
          Categories=Utility;Office;
          Keywords=text;revision;ai;
          StartupNotify=true
          StartupWMClass=Encre Settings
          EOF

          # Build deb package
          dpkg-deb --build dist/deb encre-linux-${{ matrix.arch }}.deb

      - name: Upload portable artifact
        uses: actions/upload-artifact@v4
        with:
          name: encre-linux-${{ matrix.arch }}-portable
          path: encre-linux-${{ matrix.arch }}.tar.gz

      - name: Upload AppImage artifact
        if: matrix.arch == 'amd64'
        uses: actions/upload-artifact@v4
        with:
          name: encre-linux-${{ matrix.arch }}-appimage
          path: encre-linux-${{ matrix.arch }}.AppImage

      - name: Upload deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: encre-linux-${{ matrix.arch }}-deb
          path: encre-linux-${{ matrix.arch }}.deb

  build-windows:
    strategy:
      matrix:
        arch: [amd64]
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ env.RUST_VERSION }}
          override: true

      - name: Cache Rust dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            rust-ffi/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install MinGW
        run: |
          choco install mingw -y

      - name: Add Rust GNU target
        run: rustup target add x86_64-pc-windows-gnu

      - name: Install Fyne CLI
        run: go install fyne.io/tools/cmd/fyne@v1.7.2

      - name: Install dependencies
        run: go mod download

      - name: Build Rust FFI library
        shell: bash
        run: |
          bash scripts/build-rust-ffi.sh x86_64-pc-windows-gnu
          mkdir -p lib
          cp rust-ffi/target/x86_64-pc-windows-gnu/release/libencre_ffi.a lib/

      - name: Build application with Fyne
        env:
          CGO_ENABLED: 1
          GOOS: windows
          GOARCH: ${{ matrix.arch }}
        run: |
          # Extract version from tag or use 0.0.0 for manual dispatch (fyne requires x.y.z format)
          if ("${{ github.ref }}" -like "refs/tags/*") {
            $VERSION = "${{ github.ref_name }}" -replace '^v',''
          } else {
            $VERSION = "0.0.0"
          }
          $BUILD_NUMBER = "${{ github.run_number }}"

          Write-Host "Building version: $VERSION (build: $BUILD_NUMBER)"

          # Use fyne package with version info
          fyne package --icon assets/icon.ico --name Encre --release `
            --app-version "$VERSION" `
            --app-build "$BUILD_NUMBER"

          # fyne package creates Encre.exe, rename it
          if (Test-Path "Encre.exe") {
            Move-Item Encre.exe encre-windows-${{ matrix.arch }}.exe
          }

      - name: Generate Windows README
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          @"
          Encre for Windows

          Thank you for installing Encre, the Fyne-powered AI text revision assistant.

          Getting Started:
          1. Launch encre.exe (or use the Start Menu shortcut).
          2. Right-click the tray icon to open Settings.
          3. Configure your AI provider (OpenAI, Claude, or Gemini) and hotkeys.

          Default Hotkeys:
          - Ctrl+Alt+Space: Revise all text.
          - Ctrl+Win: Revise selected text.

          Tips:
          - Enable "Start on login" from Settings > System to launch Encre automatically.
          - Encre runs from the system tray; use the tray menu to quit or update preferences.

          Support: https://github.com/paradoxe35/encre
          "@ | Out-File -FilePath dist\README.txt -Encoding UTF8

      - name: Create portable ZIP
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist\portable | Out-Null
          Copy-Item encre-windows-${{ matrix.arch }}.exe dist\portable\encre.exe -Force
          if (Test-Path "assets\icon.ico") {
            Copy-Item assets\icon.ico dist\portable\icon.ico -Force
          }
          if (Test-Path "dist\README.txt") {
            Copy-Item dist\README.txt dist\portable\README.txt -Force
          }
          Compress-Archive -Path dist\portable\* -DestinationPath encre-windows-${{ matrix.arch }}-portable.zip -Force

      - name: Create installer (NSIS)
        if: matrix.arch == 'amd64'
        shell: pwsh
        run: |
          choco install nsis -y
          $env:Path += ";C:\Program Files (x86)\NSIS"

          # Verify required files exist
          if (-not (Test-Path "encre-windows-${{ matrix.arch }}.exe")) {
            Write-Error "Source executable not found: encre-windows-${{ matrix.arch }}.exe"
            exit 1
          }
          if (-not (Test-Path "assets\icon.ico")) {
            Write-Warning "Icon file not found: assets\icon.ico - NSIS will fail"
          }

          $version = "${{ github.ref_name }}" -replace '^v','' -replace '[^0-9a-zA-Z.]','-'
          if ([string]::IsNullOrEmpty($version)) { $version = "0.0.0-dev" }

          # Create NSIS installer script
          @"
          Unicode true
          !include "MUI2.nsh"
          !include "nsDialogs.nsh"
          !include "LogicLib.nsh"

          !define PRODUCT_NAME "Encre"
          !define PRODUCT_VERSION "$version"
          !define PRODUCT_PUBLISHER "Encre Team"
          !define PRODUCT_WEB_SITE "https://github.com/paradoxe35/encre"
          !define PRODUCT_INSTALL_REG "Software\`${PRODUCT_PUBLISHER}\`${PRODUCT_NAME}"
          !define PRODUCT_UNINST_KEY "Software\Microsoft\Windows\CurrentVersion\Uninstall\`${PRODUCT_NAME}"

          Name "`${PRODUCT_NAME}"
          OutFile "Encre-Setup.exe"
          InstallDir "`$LOCALAPPDATA\`${PRODUCT_NAME}"
          InstallDirRegKey HKCU "`${PRODUCT_INSTALL_REG}" "InstallDir"
          RequestExecutionLevel user
          SetCompress auto

          !define MUI_ABORTWARNING
          !define MUI_ICON "assets\icon.ico"
          !define MUI_UNICON "assets\icon.ico"

          Function .onInit
            SetShellVarContext current
          FunctionEnd

          !insertmacro MUI_PAGE_WELCOME
          !insertmacro MUI_PAGE_DIRECTORY
          !insertmacro MUI_PAGE_INSTFILES
          !define MUI_FINISHPAGE_RUN "`$INSTDIR\encre.exe"
          !define MUI_FINISHPAGE_RUN_TEXT "Launch Encre now"
          !insertmacro MUI_PAGE_FINISH

          !insertmacro MUI_LANGUAGE "English"

          Section "Encre" SEC_APP
            SetOutPath "`$INSTDIR"
            File /oname=encre.exe "encre-windows-${{ matrix.arch }}.exe"
            File /nonfatal /oname=README.txt "dist\README.txt"
            File /nonfatal /oname=icon.ico "assets\icon.ico"

            Call RegisterApplication

            CreateDirectory "`$SMPROGRAMS\`${PRODUCT_NAME}"
            CreateShortcut "`$SMPROGRAMS\`${PRODUCT_NAME}\`${PRODUCT_NAME}.lnk" "`$INSTDIR\encre.exe"
            CreateShortcut "`$SMPROGRAMS\`${PRODUCT_NAME}\Uninstall `${PRODUCT_NAME}.lnk" "`$INSTDIR\Uninstall.exe"
            CreateShortcut "`$DESKTOP\`${PRODUCT_NAME}.lnk" "`$INSTDIR\encre.exe"
          SectionEnd

          Section "Uninstall"
            Call un.StopApplication

            Delete "`$INSTDIR\encre.exe"
            Delete "`$INSTDIR\README.txt"
            Delete "`$INSTDIR\icon.ico"
            Delete "`$INSTDIR\Uninstall.exe"
            RMDir "`$INSTDIR"

            Delete "`$DESKTOP\`${PRODUCT_NAME}.lnk"
            Delete "`$SMPROGRAMS\`${PRODUCT_NAME}\`${PRODUCT_NAME}.lnk"
            Delete "`$SMPROGRAMS\`${PRODUCT_NAME}\Uninstall `${PRODUCT_NAME}.lnk"
            RMDir "`$SMPROGRAMS\`${PRODUCT_NAME}"

            DeleteRegKey HKCU "`${PRODUCT_UNINST_KEY}"
            DeleteRegKey HKCU "`${PRODUCT_INSTALL_REG}"
          SectionEnd

          Function RegisterApplication
            WriteUninstaller "`$INSTDIR\Uninstall.exe"
            WriteRegStr HKCU "`${PRODUCT_UNINST_KEY}" "DisplayName" "`${PRODUCT_NAME}"
            WriteRegStr HKCU "`${PRODUCT_UNINST_KEY}" "UninstallString" "`$INSTDIR\Uninstall.exe"
            WriteRegStr HKCU "`${PRODUCT_UNINST_KEY}" "DisplayIcon" "`$INSTDIR\encre.exe"
            WriteRegStr HKCU "`${PRODUCT_UNINST_KEY}" "DisplayVersion" "`${PRODUCT_VERSION}"
            WriteRegStr HKCU "`${PRODUCT_UNINST_KEY}" "Publisher" "`${PRODUCT_PUBLISHER}"
            WriteRegStr HKCU "`${PRODUCT_UNINST_KEY}" "URLInfoAbout" "`${PRODUCT_WEB_SITE}"
            WriteRegStr HKCU "`${PRODUCT_UNINST_KEY}" "InstallLocation" "`$INSTDIR"
            WriteRegDWORD HKCU "`${PRODUCT_UNINST_KEY}" "NoModify" 1
            WriteRegDWORD HKCU "`${PRODUCT_UNINST_KEY}" "NoRepair" 1

            WriteRegStr HKCU "`${PRODUCT_INSTALL_REG}" "InstallDir" "`$INSTDIR"
            WriteRegStr HKCU "`${PRODUCT_INSTALL_REG}" "Version" "`${PRODUCT_VERSION}"
          FunctionEnd

          Function un.StopApplication
            nsExec::ExecToStack 'taskkill /F /IM encre.exe'
            Pop `$0
          FunctionEnd
          "@ | Out-File -FilePath installer.nsi -Encoding UTF8

          Write-Host "NSIS script created, contents:"
          Get-Content installer.nsi | Select-Object -First 20
          Write-Host "..."
          Get-Content installer.nsi | Select-Object -Last 20

          Write-Host "`nCompiling NSIS installer..."
          makensis installer.nsi
          if ($LASTEXITCODE -ne 0) {
            Write-Error "NSIS compilation failed with exit code $LASTEXITCODE"
            exit 1
          }

          if (Test-Path "Encre-Setup.exe") {
            Write-Host "Installer created successfully"
            Move-Item Encre-Setup.exe encre-windows-${{ matrix.arch }}-installer.exe -Force
          } else {
            Write-Error "Encre-Setup.exe was not created by NSIS"
            exit 1
          }

      - name: Upload portable artifact
        uses: actions/upload-artifact@v4
        with:
          name: encre-windows-${{ matrix.arch }}-portable
          path: encre-windows-${{ matrix.arch }}-portable.zip

      - name: Upload installer artifact
        if: matrix.arch == 'amd64'
        uses: actions/upload-artifact@v4
        with:
          name: encre-windows-${{ matrix.arch }}-installer
          path: encre-windows-${{ matrix.arch }}-installer.exe

      - name: Upload exe artifact
        uses: actions/upload-artifact@v4
        with:
          name: encre-windows-${{ matrix.arch }}-exe
          path: encre-windows-${{ matrix.arch }}.exe

  build-macos:
    strategy:
      matrix:
        arch: [amd64, arm64]
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ env.RUST_VERSION }}
          override: true

      - name: Cache Rust dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            rust-ffi/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Add Rust target for architecture
        run: |
          if [ "${{ matrix.arch }}" = "arm64" ]; then
            rustup target add aarch64-apple-darwin
          else
            rustup target add x86_64-apple-darwin
          fi

      - name: Install Fyne CLI
        run: go install fyne.io/tools/cmd/fyne@v1.7.2

      - name: Install dependencies
        run: go mod download

      - name: Build Rust FFI library
        run: |
          mkdir -p lib
          if [ "${{ matrix.arch }}" = "arm64" ]; then
            TARGET=aarch64-apple-darwin
          else
            TARGET=x86_64-apple-darwin
          fi
          bash scripts/build-rust-ffi.sh "$TARGET"
          cp rust-ffi/target/"$TARGET"/release/libencre_ffi.a lib/

      - name: Build application with Fyne
        env:
          CGO_ENABLED: 1
          GOOS: darwin
          GOARCH: ${{ matrix.arch }}
        run: |
          # Extract version from tag or use 0.0.0 for manual dispatch (fyne requires x.y.z format)
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.0"
          fi
          BUILD_NUMBER="${GITHUB_RUN_NUMBER}"

          echo "Building version: ${VERSION} (build: ${BUILD_NUMBER})"

          # Use fyne package with version info
          fyne package --icon assets/icon.png --name Encre --app-id me.pngwasi.encre --release \
            --app-version "${VERSION}" \
            --app-build "${BUILD_NUMBER}"

      - name: Verify .app bundle created
        run: |
          if [ ! -d "Encre.app" ]; then
            echo "Error: Encre.app was not created"
            ls -la
            exit 1
          fi
          echo "Encre.app created successfully"

      - name: Verify Info.plist
        run: |
          # Verify Info.plist exists and is valid
          echo "Info.plist contents:"
          /usr/libexec/PlistBuddy -c "Print :CFBundleExecutable" Encre.app/Contents/Info.plist
          /usr/libexec/PlistBuddy -c "Print :CFBundleIdentifier" Encre.app/Contents/Info.plist

      - name: Ad-hoc sign app bundle (inside-out approach)
        run: |
          echo "Ad-hoc signing Encre.app (without hardened runtime/entitlements)..."

          # Determine actual executable name from Info.plist
          EXEC=$(/usr/libexec/PlistBuddy -c "Print :CFBundleExecutable" Encre.app/Contents/Info.plist)
          echo "CFBundleExecutable=$EXEC"

          # Sign from inside-out (Apple best practice):
          # 1. Sign any nested frameworks first (if they exist)
          echo "Signing frameworks (if any)..."
          find Encre.app/Contents/Frameworks -name "*.framework" -exec codesign --force --sign - {} \; 2>/dev/null || true

          # 2. Sign the main executable
          echo "Signing executable: ${EXEC}"
          codesign --force --sign - "Encre.app/Contents/MacOS/${EXEC}"

          # 3. Sign the entire app bundle (without --deep since contents already signed)
          echo "Signing app bundle..."
          codesign --force --sign - Encre.app

          # Verify the signature
          echo "Verifying signature..."
          codesign --verify --verbose=4 Encre.app

          echo "Ad-hoc signing completed successfully"

      - name: Remove quarantine attributes
        run: |
          # Remove quarantine attribute to prevent "damaged" error
          xattr -cr Encre.app || true

      - name: Create installation README
        run: |
          cat > INSTALL_MACOS.txt <<'EOF'
          Encre for macOS - Installation Instructions

          FIRST LAUNCH:

          When you first open Encre, you'll see a security warning:
          "Encre cannot be opened because it is from an unidentified developer"

          METHOD 1 (Recommended):
          1. Right-click (or Control+click) Encre.app
          2. Select "Open" from the menu
          3. Click "Open" in the security dialog
          4. Encre will launch and appear in your menu bar (tray icon)

          METHOD 2 (System Settings):
          1. Try to open Encre normally (it will be blocked)
          2. Go to System Settings > Privacy & Security
          3. Look for "Encre was blocked" message
          4. Click "Open Anyway"
          5. Click "Open" in the confirmation dialog

          METHOD 3 (Terminal - if above methods don't work):
          1. Open Terminal (Applications > Utilities > Terminal)
          2. Type: xattr -cr
          3. Drag Encre.app into Terminal window
          4. Press Enter
          5. Open Encre normally

          IMPORTANT:
          - Encre runs in the menu bar (no Dock icon)
          - Grant Accessibility permissions when prompted for global hotkeys
          - After first launch, it will open normally

          Support: https://github.com/paradoxe35/encre
          EOF

      - name: Create ZIP archive
        run: |
          # Create zip without extended attributes to avoid Gatekeeper issues
          zip -r -X encre-darwin-${{ matrix.arch }}.zip Encre.app INSTALL_MACOS.txt

      - name: Create DMG
        run: |
          # Install create-dmg
          brew install create-dmg || true

          # Create DMG - allow failure if create-dmg has issues
          if create-dmg \
            --volname "Encre" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "Encre.app" 175 120 \
            --hide-extension "Encre.app" \
            --app-drop-link 425 120 \
            --no-internet-enable \
            "encre-darwin-${{ matrix.arch }}.dmg" \
            "Encre.app"; then
            echo "DMG created successfully"
          else
            echo "Warning: DMG creation failed, but ZIP archive is available"
          fi

      - name: Upload ZIP artifact
        uses: actions/upload-artifact@v4
        with:
          name: encre-darwin-${{ matrix.arch }}-zip
          path: encre-darwin-${{ matrix.arch }}.zip

      - name: Upload DMG artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: encre-darwin-${{ matrix.arch }}-dmg
          path: encre-darwin-${{ matrix.arch }}.dmg
          if-no-files-found: ignore

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-linux, build-windows, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Prepare release files
        run: |
          mkdir -p release

          # Move all artifacts to release folder
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" -o -name "*.deb" -o -name "*.exe" -o -name "*.dmg" -o -name "*.AppImage" \) -exec cp {} release/ \;

          # Create checksums
          cd release
          sha256sum * > checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          body: |
            Encre ${{ github.ref_name }}

            AI-powered text revision tool with global hotkeys and multi-provider support.

            ## Downloads

            **Windows**
            - Portable (x64): `encre-windows-amd64-portable.zip`
            - Installer (x64): `encre-windows-amd64-installer.exe`

            **macOS**
            - Intel (x64): `encre-darwin-amd64.dmg`
            - Apple Silicon (ARM64): `encre-darwin-arm64.dmg`

            **Linux**
            - Portable (x64): `encre-linux-amd64.tar.gz`
            - AppImage (x64): `encre-linux-amd64.AppImage`
            - Debian/Ubuntu (x64): `encre-linux-amd64.deb`

            Verify downloads with `checksums.txt`.

            ## Installation

            **Windows**: Run installer or extract portable ZIP

            **macOS**:
            1. Download and extract ZIP or open DMG
            2. First launch: Right-click Encre.app > Open > Click "Open"
            3. Or go to System Settings > Privacy & Security > Click "Open Anyway"
            4. Encre appears in menu bar (no Dock icon)
            5. Grant Accessibility permissions when prompted

            **Linux**:
            ```bash
            # AppImage
            chmod +x encre-linux-amd64.AppImage
            ./encre-linux-amd64.AppImage

            # Debian/Ubuntu
            sudo dpkg -i encre-linux-amd64.deb
            ```

            ## Quick Start

            1. Launch Encre (appears in system tray)
            2. Right-click tray icon > Settings
            3. Configure AI provider (OpenAI, Claude, or Gemini)
            4. Set hotkeys (optional)
            5. Use global hotkeys to revise text

            ## Default Hotkeys

            - Select All & Revise: `Ctrl+Alt+Space` (Linux/Windows), `Cmd+Option+Space` (macOS)
            - Revise Selection: `Ctrl+Super` (Linux), `Ctrl+Win` (Windows), `Ctrl+Cmd` (macOS)

            ## Requirements

            **Linux**:
            - X11 or Wayland session
            - **Wayland users only**: Add yourself to the `input` group for hotkeys to work:
              ```bash
              sudo usermod -aG input $USER
              # Then log out and log back in
              ```
            - X11 users: No special setup needed, hotkeys work out of the box
            - Required packages (Debian/Ubuntu):
              ```bash
              sudo apt install libgl1 libx11-6 libxext6 libxcb1 libxinerama1 libxtst6 libxdo3 libxkbcommon0 libxi6 libxcursor1 libxrandr2 libxrender1 libxfixes3 libxxf86vm1
              ```
            - Note: The .deb package installs required packages automatically

            **macOS**: macOS 10.13+, accessibility permissions required
            **Windows**: Windows 10+
          draft: false
          prerelease: false
          generate_release_notes: false
